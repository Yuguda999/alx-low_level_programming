#include "main.h"

/**
 * _abs - fimds the absolute value of a number
 *
 * @num: number to check
 *
 * Return: returns absolute value of num
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
